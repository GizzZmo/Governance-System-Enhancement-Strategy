[package]
name = "MinimalTest"
version = "0.0.1"

[dependencies]
# EMPTY! Let the build system auto-include Sui, MoveStdlib, Bridge, SuiSystem for the pinned Sui CLI version.

[addresses]
minimal_test_addr = "0x0"
