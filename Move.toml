# File: Move.toml
[package]
name = "HybridGovernance"
version = "0.0.1"

[dependencies]
# EMPTY - let Sui auto-include

[addresses]
hybrid_governance_pkg = "0x0" # Our package's placeholder
std = "0x1"
sui = "0x2"
# We could even try adding deepbook if that's the specific conflict point
# deepbook = "0xb" # This is a guess, might not be the correct named address for it
